import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      // Backend sets a global 'api' route prefix (src/main.ts), and production's
      // nginx config forwards /api/* to it without stripping the prefix — keep dev
      // proxy behavior identical, otherwise routes 404 in dev only.
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
      '/socket.io': {
        target: 'http://localhost:3000',
        ws: true,
      },
    },
  },
})
